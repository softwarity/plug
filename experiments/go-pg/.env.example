# Copy to .env and fill in. Use a CLUSTER-INTERNAL Postgres:
# PG_HOST must be a service name (e.g. odb) reachable ONLY through the cluster,
# so that a successful query proves plug intercepted the Go connect().
PG_HOST=odb
PG_PORT=5432
PG_USER=postgres
PG_PWD=
PG_DB=postgres
